using Xunit;

namespace PlanCope.Local.Api.Tests;

public sealed class EnrolmentEndpointsTests
{
    [Fact(Skip = "No existing WebApplicationFactory fixture found for Local.Api endpoints that fakes outbound Central calls and fakes the repositories; the redeem endpoint remains untested by this task.")]
    public void Redeem_endpoint_placeholder()
    {
    }
}